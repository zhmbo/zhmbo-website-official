.template[data-template=secret] {
  templateAnimation('.form .field > *', 2, duration3);
  templateAnimation('.form .status.current > *', 2, duration5);
  templateAnimation('.div > *', 15, duration7);
  templateAnimation('footer .links > *', 2, duration20);
}

.wrapper.secret {
  min-width: 390px;

  main .form .status span {
    color: green;
  }

  main .div {
    z-index: -1;
    position: relative;
    margin-top: baseline * 3;
  }
}
